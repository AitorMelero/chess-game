import { expect, test } from '@playwright/test'

test.describe('PieceModel Testing', () => {
  test('deberían asignarse correctamente los atributos durante la creación', () => {
    expect(true).toBeTruthy()
  })
})
